## About Config:
package.json:
- Сказал, какие библиотеки нужны (express, dotenv).
- Сказал, какие нужны только для разработки (typescript, tsx).
- Дал команды для запуска (npm run dev, npm run build).

tsconfig.json:
- Научил TypeScript превращать код в понятный для Node.js JavaScript.
- Включил строгий режим (strict), чтобы ловить ошибки ещё до запуска.
- Настроил пути, чтобы импорты были красивыми.